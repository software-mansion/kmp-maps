name: Build
on:
  merge_group:
  pull_request:
  push:
    branches: [main]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5
      - run: ./gradlew build -x lint -x test
